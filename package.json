{
  "name": "automationforge",
  "version": "2.0.0",
  "private": true,
  "description": "AutomationForge v2 — local agent + Netlify web dashboard",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'Static site — no build step'"
  },
  "dependencies": {
    "@netlify/blobs": "^8.2.0",
    "bcryptjs": "^2.4.3",
    "cookie": "^1.0.2",
    "jsonwebtoken": "^9.0.2"
  }
}
